<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/action_search" android:title="Home"
        android:icon="@drawable/ic_home_white_24dp" app:showAsAction="always"/>

    <item android:id="@+id/menu_addClient" android:title="Add Client"
        app:showAsAction="ifRoom"
        android:visible="false"/>


    <item android:id="@+id/action_notification" android:title="Notifications"
        android:icon="@drawable/ic_notifications_active_white_24dp"
        app:showAsAction="ifRoom"
        android:visible="false"/>
    <item android:id="@+id/action_settings" android:title="@string/app_logout"
        android:icon="@drawable/ic_action_logout"
        android:visible="false"
         app:showAsAction="ifRoom"/>
</menu>
